<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>Edit Your Post</h2>
    <p>post id : <%= post.id %> </p>
    <p>Post username : <%= post.username %> </p>
    <!-- isme esliey <%= post.id %> likhe hai because root me hai -->
     
    <!-- jis id me bhejenge wo id ka dat update karenge wahi hai becuase abhi ye id compaire hoga pher us id ke data ke content me change hoga -->
     <!-- impo -->
    <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
        <textarea name="content"><%= post.content %> </textarea>
        <button>Submit/</button>
    </form> 
</body>
</html>